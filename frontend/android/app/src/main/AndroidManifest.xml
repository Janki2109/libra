<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <!-- In-app Audio/Video consultation calls (WebRTC via flutter_webrtc). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>
    <!-- Required on Android 13+ for FCM/flutter_local_notifications to show
         a notification; FirebaseMessaging.requestPermission() prompts for it
         at runtime, but the permission must still be declared here. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!--
      READ_/WRITE_EXTERNAL_STORAGE were declared but nothing uses them:
      image_picker reads through the system picker, which needs no permission,
      and documents are uploaded as base64 rather than written to disk. Play
      Store review treats broad storage access as a sensitive permission and
      asks you to justify it, so declaring one the app never exercises is a
      rejection risk for no benefit. WRITE_EXTERNAL_STORAGE is also a no-op
      from Android 11 onward. CAMERA is declared because the lawyer document
      capture step uses ImageSource.camera, which needs it.
    -->

    <application
        android:label="Libra Law"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- Channel used for a "notification" payload delivered while the app
             is backgrounded/terminated, so it matches the channel FcmService
             creates for foreground messages. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="default_channel" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>